import { BookOpen,Brain,House,LogOut,Trophy,Upload } from 'lucide-react'
import { NavLink,Outlet } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'
export function AppShell(){const {user,profile,signOut}=useAuth();return <div className="app"><header><NavLink to="/" className="brand"><span className="brand-mark">LC</span><span>Lexi Cycle</span></NavLink><nav><NavLink to="/"><House size={18}/>首页</NavLink><NavLink to="/vocabulary"><BookOpen size={18}/>词库</NavLink><NavLink to="/upload"><Upload size={18}/>我的词库</NavLink><NavLink to="/mistakes"><Brain size={18}/>易错词</NavLink><NavLink to="/leaderboard"><Trophy size={18}/>学习榜单</NavLink></nav><div className="account"><span>{profile?.nickname??user?.email?.split('@')[0]}</span><button onClick={()=>void signOut()} title="退出登录"><LogOut size={17}/></button></div></header><main><Outlet/></main></div>}
