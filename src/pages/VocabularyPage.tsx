import { Search } from 'lucide-react'
import { useState } from 'react'
import { useApp } from '../context/AppContext'
import { DATASET_NAMES,type Dataset } from '../domain/vocabulary'
export function VocabularyPage(){const {cards}=useApp(),[query,setQuery]=useState(''),[filter,setFilter]=useState<'all'|Dataset>('all');const q=query.trim().toLowerCase(),shown=cards.filter(c=>(filter==='all'||c.dataset===filter)&&(!q||[c.lemma,c.answer,c.sentence,c.translation].some(v=>v.toLowerCase().includes(q))))
 return <div className="page library"><p className="eyebrow">VOCABULARY</p><h1>词汇库</h1><p className="page-intro">浏览全部 {cards.length} 个真实语境词条。</p><div className="toolbar"><label><Search size={18}/><input placeholder="搜索单词、例句或中文…" value={query} onChange={e=>setQuery(e.target.value)}/></label><div className="filters">{([['all','全部'],...Object.entries(DATASET_NAMES)] as [typeof filter,string][]).map(([key,name])=><button className={filter===key?'active':''} onClick={()=>setFilter(key)} key={key}>{name}</button>)}</div></div><div className="word-table"><div className="table-head"><span>词汇 / 词形</span><span>语境</span><span>类型</span></div>{shown.map(c=><div className="word-row" key={c.id}><span><b>{c.lemma}</b>{c.lemma!==c.answer&&<small>→ {c.answer}</small>}</span><span><b>{c.sentence}</b><small>{c.translation}</small></span><span className="tag">{DATASET_NAMES[c.dataset]}</span></div>)}</div><p className="result-count">显示 {shown.length} 条结果</p></div>}
