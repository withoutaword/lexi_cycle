import { Search } from 'lucide-react'
import { useState } from 'react'
import { useApp } from '../context/AppContext'
import { DATASET_NAMES,type Dataset } from '../domain/vocabulary'
const PAGE_SIZES=[10,20,50,100] as const
export function VocabularyPage(){const {cards}=useApp(),[query,setQuery]=useState(''),[filter,setFilter]=useState<'all'|Dataset>('all'),[pageSize,setPageSize]=useState<number>(20),[page,setPage]=useState(1);const q=query.trim().toLowerCase(),shown=cards.filter(c=>(filter==='all'||c.dataset===filter)&&(!q||[c.lemma,c.answer,c.sentence,c.translation].some(v=>v.toLowerCase().includes(q)))),pageCount=Math.max(1,Math.ceil(shown.length/pageSize)),currentPage=Math.min(page,pageCount),pageItems=shown.slice((currentPage-1)*pageSize,currentPage*pageSize)
 return <div className="page library"><p className="eyebrow">VOCABULARY</p><h1>词汇库</h1><p className="page-intro">浏览全部 {cards.length} 个真实语境词条。</p><div className="toolbar"><label><Search size={18}/><input placeholder="搜索单词、例句或中文…" value={query} onChange={e=>{setQuery(e.target.value);setPage(1)}}/></label><div className="filters">{([['all','全部'],...Object.entries(DATASET_NAMES)] as [typeof filter,string][]).map(([key,name])=><button className={filter===key?'active':''} onClick={()=>{setFilter(key);setPage(1)}} key={key}>{name}</button>)}</div></div><div className="word-table"><div className="table-head"><span>词汇 / 词形</span><span>语境</span><span>类型</span></div>{pageItems.map(c=><div className="word-row" key={c.id}><span><b>{c.lemma}</b>{c.lemma!==c.answer&&<small>→ {c.answer}</small>}</span><span><b>{c.sentence}</b><small>{c.translation}</small></span><span className="tag">{DATASET_NAMES[c.dataset]}</span></div>)}</div><div className="pagination"><p className="result-count">共 {shown.length} 条 · 第 {currentPage}/{pageCount} 页</p><label>每页<select value={pageSize} onChange={e=>{setPageSize(Number(e.target.value));setPage(1)}}>{PAGE_SIZES.map(size=><option value={size} key={size}>{size}</option>)}</select>条</label><div><button disabled={currentPage===1} onClick={()=>setPage(value=>Math.max(1,value-1))}>上一页</button><button disabled={currentPage===pageCount} onClick={()=>setPage(value=>Math.min(pageCount,value+1))}>下一页</button></div></div></div>}
