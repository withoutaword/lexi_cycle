import { useState,type FormEvent } from 'react'
import { Navigate } from 'react-router-dom'
import { isSupabaseConfigured,useAuth } from '../context/AuthContext'
export function AuthPage(){const {user,signIn,signUp}=useAuth(),[register,setRegister]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);if(user)return <Navigate to="/"/>
 const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setMessage('');const result=await(register?signUp(email,password):signIn(email,password));setMessage(result??'登录成功');setBusy(false)}
 return <div className="auth-page"><div className="auth-brand"><span className="brand-mark">LC</span><b>Lexi Cycle</b></div><div className="auth-panel"><p className="eyebrow">WELCOME TO LEXI CYCLE</p><h1>{register?'创建账户':'继续学习'}</h1><p>你的词库和学习进度会安全地保存在个人账户中。</p>{!isSupabaseConfigured&&<div className="config-warning">部署前请配置 Supabase 环境变量并执行数据库脚本。</div>}<form onSubmit={submit}><label>邮箱<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>密码<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="至少 6 位"/></label>{message&&<p className="form-message">{message}</p>}<button className="primary" disabled={busy||!isSupabaseConfigured}>{busy?'请稍候…':register?'注册':'登录'}</button></form><button className="text-button" onClick={()=>{setRegister(!register);setMessage('')}}>{register?'已有账户？登录':'没有账户？注册'}</button></div></div>}
